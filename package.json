{
  "name": "tree-shaking-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:named": "cd examples/named-exports && vite build",
    "build:class-optimized": "cd examples/class-examples/optimized && vite build",
    "build:class-heavy": "cd examples/class-examples/heavy && vite build",
    "clean": "rm -rf dist",
    "test": "npm run clean && npm run build:named && npm run build:class-optimized && npm run build:class-heavy && node compare-bundles.js"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}